<Window x:Class="BackupWorkstation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Workstation Backup Tool"
        Height="400"
        Width="600"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="White">

    <Window.Resources>
        <!-- Global button style -->
        <Style TargetType="Button">
            <Setter Property="Background"
                    Value="CornflowerBlue" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="Padding"
                    Value="6,3" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Title bar -->
            <RowDefinition Height="*" />
            <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Custom title bar -->
        <Border Background="CornflowerBlue"
                Height="32"
                Grid.Row="0">
            <DockPanel LastChildFill="True"
                       MouseDown="TitleBar_MouseDown">
                <TextBlock Text="Workstation Backup Tool"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"
                           FontWeight="Bold" />
                <Button Content="X"
                        Width="32"
                        Height="32"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"
                        HorizontalAlignment="Right"
                        Click="Close_Click" />
            </DockPanel>
        </Border>

        <!-- Main content -->
        <Grid Grid.Row="1"
              Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Input Fields -->
            <StackPanel Orientation="Horizontal"
                        Margin="0,0,0,5">
                <TextBlock Text="Backup Destination:"
                           VerticalAlignment="Center"
                           Margin="0,0,5,0" />
                <TextBox x:Name="txtBackupPath"
                         Width="350"
                         Margin="0,0,5,0" />
                <Button Content="Browse..."
                        Width="75"
                        Click="Browse_Click" />
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        Grid.Row="1"
                        Margin="0,0,0,5">
                <TextBlock Text="Source Username:"
                           VerticalAlignment="Center"
                           Margin="0,0,15,0" />
                <TextBox x:Name="txtUsername"
                         Width="350" />
            </StackPanel>

            <!-- Live Log with watermark overlay -->
            <Grid Grid.Row="2">
                <ListBox x:Name="lstLog"
                         FontFamily="Consolas" />
                <Image Source="pack://application:,,,/Assets/AdvTechLogo.png"
                       Opacity="0.15"
                       Stretch="Uniform"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Bottom"
                       Margin="5"
                       IsHitTestVisible="False"
                       Width="{Binding ActualWidth, ElementName=lstLog}"
                       Height="{Binding ActualHeight, ElementName=lstLog}"
                       Panel.ZIndex="1" />
            </Grid>

            <!-- Progress + Start -->
            <StackPanel Orientation="Horizontal"
                        Grid.Row="3"
                        Margin="0,5,0,0">
                <ProgressBar x:Name="progressBar"
                             Width="400"
                             Height="20"
                             Margin="0,0,25,0" />
                <Button Content="Start Backup"
                        Width="120"
                        Click="StartBackup_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>